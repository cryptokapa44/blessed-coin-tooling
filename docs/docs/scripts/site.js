// site.js – Blessed Coin basic script
// This will later handle live token stats, quests, and animations.

console.log("Blessed Coin site loaded");

// Example placeholder for token data
const tokenStats = {
  symbol: "BLESS",
  marketCap: "Loading...",
  price: "Loading...",
};

document.addEventListener("DOMContentLoaded", () => {
  console.log("Page ready – token stats:", tokenStats);
});
